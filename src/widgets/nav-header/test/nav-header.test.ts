import {beforeEach, describe, expect, it} from 'vitest';
import { render } from '../../../../test-setup/test-utils';



describe('header-link testing', () => {
  

    // beforeEach(async () => {
    //     await import('../index');
    //     render(`<nav-header></nav-header>
    //     <input-search></input-search>
    //     `)
    // })

    it('Check the nav-header component rendering DOM', () => {
        // const element = document.querySelector('nav-header').shadowRoot
        // expect(element).toBeInTheDocument()
        expect(1).toBe(1)
    })

    // it('Check nav-header component structure', () => {
    //     const shadow = document.querySelector('nav-header').shadowRoot
    //     const element = shadow.querySelector('.nav-header')
        

    //     expect(shadow).not.toBeNull()
    //     expect(element).toBeInTheDocument()
        
    // })

})
